# AdamAddem.github.io
A simple GitHub Pages site for the repository.

To preview locally:
1. In the repo root run: python3 -m http.server 8000
2. Open http://localhost:8000

To publish:
- This repository is already named `AdamAddem.github.io` — push to `main` (or `master`) and GitHub Pages will serve the site at https://AdamAddem.github.io
